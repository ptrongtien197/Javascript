/**
 * Giới thiệu hàm built-in
 * alert
 * console
 * confirm
 * prompt
 * set timeout
 * set interval
 */

var a = 8,
    b = 3;

// console.log(a ** b);

var result = false || "A" || "B" || "C";

console.log(result);
